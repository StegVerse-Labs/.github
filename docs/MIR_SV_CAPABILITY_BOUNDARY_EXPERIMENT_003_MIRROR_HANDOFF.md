# MIR / StegVerse Experiment 3 — canonical mirror handoff

Updated: 2026-09-24
Goal Task ID: `MIR-SV-CAPABILITY-BOUNDARY-EXPERIMENT-003`
COSV ID: `50000000100000` (task.v1, documentary/source active; no claim of authentic runtime)
Registry source generation at intake: 226; canonical registration MERGED at generation 227 and independently read back from main (one Registry row, one COSV index row, exact task vector 50000000100000).
Status: ACTIVE / UNCLAIMED / REGISTRATION MERGED / EXACT-HEAD SOURCE-CI VALIDATED / STEGVERSE COUNTERPART SOURCE AUDIT DRAFTED / AUTHENTIC RUNTIME NOT ESTABLISHED
Owner issue: https://github.com/StegVerse-Labs/.github/issues/2682

## Experiment identity and separation
New MIR/SV Experiment 3 is the *independent capability-boundary and collaboration assessment*, **not** Seam Experiment 1 (existing seam), Seam Experiment 2 (v0.8 under development/frozen contract pending), SV Test/Demo 3 (40-second worker), or SV Test/Demo 4 (manifold capability). The later proposed third-framework execution / StegVerse commit-time-admissibility / MIR participation-history experiment is a subsequent, separately sequenced experiment. Richard undertook to notify Emanuel before that interoperability exercise, without granting Emanuel authority over this bilateral work.

## Agreed four questions
1. What was claimed?
2. What was independently observed?
3. What physical work actually occurred?
4. What remains unknown?

Each independent party must also inventory what it can **observe, demonstrate, retain, and reconstruct**. Capabilities and limitations carry equal weight **inside the same artifact**, not footnotes. Keep independent authority, attribution, dissent and observational limitations intact. A reporting gap (activity occurred but went unsubmitted) and an observability limit (no participating observer could see it) are distinct. A declared external coverage obligation makes silence checkable; sampling from activity-generating systems rather than solely the registry is required to discover omitted submissions. Retain each participant's unknowns and disagreements as separately attributable first-class entries; union of observed blind spots does not certify completeness when participants share an unobservable domain.

## Richard's delivered MIR-side baseline
Source: Richard Whitney, MIRegistry LLC, *Experiment One: what MIR can observe, demonstrate, retain and reconstruct*, 24 September 2026, user-supplied five-page PDF; SHA-256 `1e2f134ab60144bb4c5a4de135c6f76d460f22995a0e0b2df1ef071f7e8c8ffe`. Supplied LinkedIn thread screenshot hashes and a bounded reconstruction of their visible text are in `docs/mir-exp3/RECOVERED_CONVERSATION_EVIDENCE.md`. Original bytes were user-uploaded in this conversation, not committed as binary to GitHub; immutable hashes identify originals and the recovered transcription is not a substitute for them.

Richard distinguishes no external partner observation, independently checkable alteration evidence (not event truth/completeness), indefinite-by-default but historically destructive retention and limited leaf/chain reconstruction (not incident/state/causation reconstruction). Numerical production measurements remain `COUNTERPART_REPORTED` unless independently remeasured; endpoint commands are independently reproducible where public.

## StegVerse counterpart
Draft: `docs/mir-exp3/STEGVERSE_SIDE_OBSERVE_DEMONSTRATE_RETAIN_RECONSTRUCT.md`. Source-verified constitutional contracts are distinct from previously reported source/CI outcomes and authentic independently retrieved organization/Master Records runtime receipts. The draft gives limitation and reproducibility instruction adjacent to each capability. No claim that sovereign resident runtime was exercised in this documentation task.

## Source-backed audit boundaries
- Existing canonical parent `MIR-STEGVERSE-SEPARATION-OF-POWERS-EVIDENCE-CONTRACT-001` remains separate and has exhausted 20 goal prompts; architecture reference only.
- Existing `CANONICAL-MASTER-RECORDS-STATE-TRANSITION-CUSTODY-001` is authoritative source for canonical state receipts, required evidence and reconstruction, not evidence that any particular external transition happened.
- Existing MIR browser source handoff says current MIR authentic Node ingress, EVENT_EPHEMERAL run, RTC-007/008/009 and governed-return receipts were not observed at its 2026-09-17 measurement. Re-observe current retained organization-ledger HEAD and immutable receipts before promoting any later outcome.
- Existing StegVerse-002 successful execution route is a previously documented fixture, not current MIR/SV Exp3 execution. GitHub/GitHub Actions have no runtime authority.
- Preserve MIR v0.8 seam figures and historical run claims in their own evidence classification; never merge their measurements into this new Exp3.

## Verification and next transition
1. Validate source files and Registry COSV/record/index consistency at exact PR head and rebase/reconcile if current Registry advances.
2. Obtain independent current read-only evidence from the existing authorized organization ledger HEAD and immutable receipt chain, followed by actual corresponding canonical Master Records responses; report `NOT_OBSERVED_IN_ACCESSIBLE_SURFACE` rather than asserting no runtime occurred where access is absent.
3. For each positive claim, provide exact reproducible source file, public endpoint or retained receipt identifier plus command/query and proof ceiling; for each limitation, show counterexample, explicit coverage constraint or retained negative evidence. Classify source/CI, historical reported, independent external and authentic current runtime separately.
4. Return the StegVerse counterpart for joint review. Do not begin the separate third-framework interoperability test or rewrite the existing frozen contracts.

## Canonical registration readback (2026-09-24)

[Registration PR #2683](https://github.com/StegVerse-Labs/.github/pull/2683) merged as `33602d67d50e57d1bcff4b2f77a0a05ed4b1ad36` from exact head `efe80c70e9f816fcc76db1402e049c156925a634`. All seven observed exact-head workflows completed SUCCESS: Task Registry Work Correlation `36083682537`, Cross-Task Coordination `36083682598` (initial missing execution-substrate-review failure repaired by non-executing documentary classification, then green), Deterministic Repository Suite `36083682563`, Purpose-Bound Worker `36083682475`, KV AI Memory `36083682600`, STCM CHF registration `36083682643`, and DeepSeek resident source `36083682471`. Main readback confirms canonical Registry generation 227, exactly one task row, one COSV index entry and matching vector `50000000100000`. These are source/coordination validation only. No authentic current resident organization-ledger, Master Records runtime or external physical consequence was observed by this experiment.

Original PDF and ten screenshot bytes remain retained in the user's conversation and bundled in a user-downloadable `mir-sv-exp3-original-evidence.zip` with SHA256SUMS. Binary original artifacts were not written to the GitHub repository; the versioned GitHub provenance note carries exact SHA256 and a bounded visible-content reconstruction.

## Complete Richard pre-PDF source recovered (2026-09-24, follow-up)

Rigel supplied Richard Whitney's **complete text** of the pre-PDF response. It is preserved verbatim at `docs/mir-exp3/RICHARD_PRE_PDF_EXACT_MESSAGE.md` and the source inventory now distinguishes that complete message from cropped screenshots and the subsequent MIR PDF. This provides exact counterpart language: Experiment 3 is a substantive empirical capability/limitation assessment, not a preliminary warm-up; equal-weight limits belong in the same artifact; a later **separate** third-framework interoperability study would test MIR actor-participation standing against independently observed commit-time decisions and unaccounted before/after residue, accepting null/negative findings. Richard's promise to notify Emanuel before the **later** experiment is a sequencing/disclosure commitment about the separate Emanuel/Justin/Richard formation, not approval-seeking or a restriction on this bilateral first experiment. No notification or later experiment execution is claimed. Preserve the pre-PDF wording as historical speech and prefer the subsequently submitted MIR PDF for MIR's measured side of the present experiment. This documentary recovery does not advance current resident organization-custody, physical-work or Master Records evidence predicates.

## SDK manifested counterpart — tested and merged (2026-09-24)

The operational next step is now implemented as SDK manifest source work, rather than another unbounded read-only receipt search. [SDK PR #317](https://github.com/StegVerse-org/StegVerse-SDK/pull/317) merged as `4c709c6e376647c75b3ff685b4829fe1f8110fc5` from exact tested head `2508068f5475364ed4b47db6f6349e9b91fdb290` after **all 12 applicable workflows succeeded**, including dedicated [MIR/SV Exp3 manifest workflow #36085026183](https://github.com/StegVerse-org/StegVerse-SDK/actions/runs/36085026183). Six new focused tests passed, including missing limitation mutation and negative assertions preventing unobserved runtime or physical-work promotion.

- [Manifest source input](https://github.com/StegVerse-org/StegVerse-SDK/blob/main/inspection/examples/mir-sv-exp3/assessment-input.json) embeds the four capability/limitation/independent-check sections plus the four agreed questions and first-class unknowns.
- [SDK diagnostic request](https://github.com/StegVerse-org/StegVerse-SDK/blob/main/inspection/examples/mir-sv-exp3/diagnostic-request.json) contains 4 source-contract declaration checks and 8 deliberately `NOT_OBSERVED` authentic current/physical/coverage checks.
- [Manifest generator](https://github.com/StegVerse-org/StegVerse-SDK/blob/main/scripts/build_mir_sv_exp3_manifest.py) invokes existing installed Manifest Builder with `process=ecosystem_diagnostic`, `return_depth=full-trace`, and a declared Publisher stage. No new runtime, authority or processor added.
- Actual immutable [workflow artifact](https://github.com/StegVerse-org/StegVerse-SDK/actions/runs/36085026183/artifacts/10843328013) contains the generated `mir-sv-exp3.manifest.json` (SHA-256 `e1b05a082ce19d3d254e3cde1dced03019174a94287724959672c9e65510c8f3`) and the read-only `mir-sv-exp3.diagnostic-result.json` (SHA-256 `5acde471f793546a7a457ab11cbe142aeefa6637e747550390f1db213cfaeaea`). Original artifact ZIP SHA-256 `911ca6f07622c4bc603ee8acbb544ef7eb4c0c9cf624ff8abaf9fb8dc4254417` per workflow upload log.

**Actual SDK execution boundary:** The initial attempt to use public `run-manifest` inside isolated source CI returned `UNIVERSAL_INTR_INGRESS_NOT_CONFIGURED`, establishing that the real public route requires actual configured Interlock/InTr ingress. The repair preserves that fail-closed boundary: the SDK's **existing local, non-authorizing** `ecosystem_diagnostic_runtime.execute_manifest` produces a source-scoped diagnostic result after canonical Manifest Builder validation. It does not invoke the governed `run-manifest` runtime, actual Publisher, Master Records or far-side destination and is **not** authentic resident transition proof. Future actual current-runtime claims require authorized organization HEAD and hash-linked receipts with same-transition canonical Master Records closure, followed by independent comparison. No real physical measurement exists in the present source-only input. The current experiment did not run the separately sequenced third-framework standing experiment.

## Review-default Publisher seam / original evaluator intake (2026-09-24)

Following user confirmation: **Publisher is optional for ordinary SDK runs but the default for runs explicitly intended to return evidence to others for review.** The existing publisher contract remains a manifest-declared optional stage, never a universal obligation or new authority. Existing generic owner repair [Publisher PR #73](https://github.com/GCAT-BCAT-Engine/Publisher/pull/73) merged as `a4114e7d95ba8999030c06e22ec2cf92ccbee649` from tested head `50b0b59cdf44bfedc4aaa33a4e82996b010dd30e`; all six observed applicable workflows passed, including exact original evaluator assets. That PR reuses `publisher/intr_artifact_transfer.py` and `publisher/document_pipeline.py`, adds exact optional `evaluator_assets` under the **same** artifact manifest/rendering receipt, and permits the explicitly distinct generic `stegverse.publisher.evidence-report-package/v1` source without mislabeling GitHub/evaluator sources as private KV origin. The specialized historical MIR round-trip capsule remains restricted to its own original goal.

Existing SDK owner repair [SDK PR #318](https://github.com/StegVerse-org/StegVerse-SDK/pull/318) merged as `949ade7d9870da3a3aebcc887ebd6687742f012f` from exact head `1cdba2dce3cfcd325470b8f61d0418bad600a8dc` after **all 15 observed applicable PR workflows succeeded**, including Manifest Builder, Publisher SDK Return Binding and SDK Package Artifact validation. SDK Manifest Builder now supports `external_review=True` / CLI `--external-review`, which defaults `completion.publisher.required=true` for reviewer-facing **new** manifests; ordinary manifests remain Publisher-optional and explicit `--no-publisher` overrides review default. New `stegverse/review_publisher_transfer.py` prepares the **existing** canonical `stegverse.publisher.artifact-transfer/v1` envelope from an already-authorized generic source export and a complete exact-byte original evidence list, rejects missing/mutated originals, and checks returned transfer/export/manifest consistency before calling the **existing** SDK return assembler. The generated packet is `PREPARED_NOT_TRANSPORTED` until authentic InTr transport. See [SDK review-transfer contract](https://github.com/StegVerse-org/StegVerse-SDK/blob/main/docs/SDK_EXTERNAL_REVIEW_PUBLISHER_TRANSFER.md) and [Publisher owner contract](https://github.com/GCAT-BCAT-Engine/Publisher/blob/main/docs/SDK_REVIEW_PUBLISHER_EVIDENCE_MIRROR_HANDOFF.md).

**Experiment 3 application:** the previously supplied PDF and ten original PNGs were independently SHA-256 checked 11/11 against the existing canonical evidence manifest. A local **reviewer source-intake package** was produced in this conversation (`mir-sv-exp3-reviewer-intake.zip`, exact ZIP SHA-256 `f7cb383094573eac08fa0dfcb15768e4521e093d74c96931609f0f0daaf0008a`) containing all eleven original bytes, an evaluator source inventory and an SDK-transfer-compatible base64 `evaluator-assets.json`. The zip remains **local conversation output**, not an authorized publisher transfer, public release or persistent GitHub binary. Its original PDF hash remains `1e2f134ab60144bb4c5a4de135c6f76d460f22995a0e0b2df1ef071f7e8c8ffe` and image hashes remain in `RECOVERED_CONVERSATION_EVIDENCE.md`. The original frozen SDK manifest and diagnostic from PR #317 remain unchanged; a future review-facing manifest must be its new independently identified successor.

**Authentic runtime remaining:** this environment cannot retrieve authentic resident Universal InTr ingress/organization-ledger HEAD, matching Master Records closure or actual Publisher destination/return/far-side receipts. The old isolated source-CI failure `UNIVERSAL_INTR_INGRESS_NOT_CONFIGURED` is an environment-specific observation, **not evidence that resident InTr is universally unavailable**. Neither Publisher PR #73 nor SDK PR #318 claims to have transported the original Experiment 3 user bytes through the sovereign path; the source-only packet/demo cannot stand in for authentic Publisher production/custody or evaluator delivery. These remain explicit open experiment evidence predicates. The distinction between independently observed physical work and reported/software activity is unchanged.

## Reviewer intake exact-root strengthening and original SDK artifact inclusion (2026-09-24)

Reused the **same** SDK and Publisher ownership. SDK [PR #320](https://github.com/StegVerse-org/StegVerse-SDK/pull/320) merged as `08ca74b02f0333b5ca37546b685f23c53ec0df38` from exact head `9adfb0c041432f7745e88bf4237a60288cbc6162`, after two triggered source workflows succeeded. The SDK generic transfer preparer now rejects any authorized report export whose `source.verification_root` is not the SHA-256 of the **unchanged original wire manifest's canonical JSON**. The generic return consumer checks actual artifact-manifest digest, rendering-receipt digest and their linkage, and equality of all original/returned artifact paths before calling the existing SDK return assembler. No separate runtime or authority was added. The prior SDK [PR #318](https://github.com/StegVerse-org/StegVerse-SDK/pull/318) established default Publisher only on explicit external review, optional otherwise; Publisher [PR #73](https://github.com/GCAT-BCAT-Engine/Publisher/pull/73) owns exact original assets.

Actual original [SDK Exp3 workflow artifact](https://github.com/StegVerse-org/StegVerse-SDK/actions/runs/36085026183/artifacts/10843328013) was retrieved and inspected byte-for-byte. The unchanged source-only manifest JSON has file SHA-256 `e1b05a082ce19d3d254e3cde1dced03019174a94287724959672c9e65510c8f3`, and its **canonical wire JSON root** (used as `source.verification_root`) is `ad9b8b8aab2beeea04bff2aac34fd2e7bfa5915133bcaef9209c16de7d9bea68`; distinguish these two hashes. The immutable diagnostic JSON file SHA-256 is `5acde471f793546a7a457ab11cbe142aeefa6637e747550390f1db213cfaeaea` and declares `NONE_DIAGNOSTIC_ONLY` authority. A self-contained **local** evaluator source package was then produced for the user (`mir-sv-exp3-evaluator-source-package.zip`, SHA-256 `3b2cb045bc37afae5bff7927cfb4ca1794c56f5bf3f0f3f04c2934632b36ffe0`) comprising Richard's original PDF and ten original PNG screenshots, complete directly supplied pre-PDF message, exact original SDK manifest and diagnostic result, an in-artifact 4-section source assessment, an original-asset base64 inventory and per-file SHA256SUMS. All 11 original assets and all package entries passed local SHA256 readback. The archive is a **private conversation output**, not a public release, GitHub binary upload, a canonical Publisher return or an authenticated destination receipt. Richard's original PDF bytes remain unchanged; the conversation screenshots retain their crop/authenticity limitations.

**Current authentic transition truth:** local package = `SOURCE_ONLY_PREPARED_NOT_TRANSPORTED`; actual Publisher source path = implemented/tested; real Experiment 3 Universal InTr transport, Publisher destination/return receipts, matched current Master Records closure, SDK consumer return and far-side evaluator delivery = `UNVERIFIED`. Do not re-open merged source-only repairs or promote a fabricated or locally prepared packet into live execution evidence. Use actual existing organization-level custody when authorized evidence is accessible; identify the first genuine failed existing transition if it is not.

## Shared SDK→Publisher reviewer-default repair and actual runtime ceiling (2026-09-24)

**User policy now implemented:** Ordinary SDK manifests permit Publisher to be omitted. Caller-declared external review defaults Publisher to required, with explicit opt-out. Existing SDK [PR #318](https://github.com/StegVerse-org/StegVerse-SDK/pull/318) established the non-duplicated policy and source producer. Redundant overlapping SDK PR #319 was CLOSED without merge. Existing Publisher [PRs #73](https://github.com/GCAT-BCAT-Engine/Publisher/pull/73), [#74](https://github.com/GCAT-BCAT-Engine/Publisher/pull/74) and [#75](https://github.com/GCAT-BCAT-Engine/Publisher/pull/75) merged: generic non-KV-origin reviewer report, original exact-byte screenshot/PDF attachments included in the same artifact manifest and artifact-return receipt, exact source-profile identifier for correct reverse-owner selection, and distinct SDK source-validated evidence class. Historical specialized MIR and KV authority boundaries remain unchanged.

Existing SDK [PR #321](https://github.com/StegVerse-org/StegVerse-SDK/pull/321) merged as `5b6f7aa70195ad556f539cf3db415db070fbc6be` after four applicable exact-head workflows passed (including cross-owner real Publisher source adapter test). It reuses the already-merged generic SDK review transfer (does **not** create duplicate transport), adds strict generic SDK-side manifest/receipt hash checks, complete exact artifact coverage and source provenance, and separates source-validated manifest/diagnostic from authentic runtime evidence.

Existing StegOS [PR #411](https://github.com/StegVerse-Labs/StegOS/pull/411) merged as `ca108145fd9ff4a69d45a2fabe0b025a4c8a14b0` after the full StegOS CI suite passed. It adds a **data-only profile on the existing Universal InTr**: `sdk-publisher-review` uses same-ecosystem `SDK:ReviewerEvidenceExport → Publisher:Ingress` and `Publisher:Export → SDK:ReviewerReturn` exact-byte paths without adding a runtime, authority plane, connected device or second user-operated device. Existing KV-origin profile remains unchanged. Exact regenerated connector manifests and browser mirror are reconciled.

Existing central shared Publisher forward consumer [PR #2694](https://github.com/StegVerse-Labs/.github/pull/2694) merged as `387a2d6a843d7311394417dc7cb687bd8686b653` after its exact-head SDK-review forward validation passed. It verifies the actual source payload schema and qualified StegOS connector intent/packet hash, rejects generic SDK transfers on the KV path or misuse of the specialized MIR owner binding, and selects SDK as the generic Publisher return owner. Owner-scoped handoff: `docs/SDK_REVIEW_PUBLISHER_INTR_FORWARD_MIRROR_HANDOFF.md`.

**Experiment 3 private evaluator output:** additionally verified a self-contained local review archive `mir-sv-exp3-complete-evaluator-source-package.zip` with exact SHA-256 `b35d8faa1018f924fb2d1e67ca60af194ec7038befe3a732f59228059c0aa617`. It includes unchanged original MIR PDF, all ten unchanged PNG screenshots, Richard's directly supplied complete pre-PDF message, frozen SDK manifest and diagnostic, a navigable gallery, local 4-page PDF equal-weight capability/limitation review, base64 original-asset inventory and SHA256SUMS. 11/11 original SHA-256 and 4 source-PASS/8 NOT_OBSERVED SDK diagnostic predicates were rechecked; original ZIP content is private in this conversation, **not** in public GitHub and **not** an authentic Publisher-return packet. Its local PDF is an evaluator draft, not Publisher-host evidence.

**First open integrated transition (NOT AUTHENTICALLY OBSERVED):** existing shared **reverse** Publisher materialization/consumer is KV or historical MIR profile-specific and does not yet accept and reconstruct `SDK:ReviewerReturn` from the new generic same-boundary SDK profile with authenticated original SDK manifest and actual Master Records source. Forward source registration is source-validated only; it is not proof that a resident forward hop or Publisher destination has occurred, so inspect authentic resident organization ledger HEAD and relevant immutable ingress/Publisher/return entries before assigning a concrete runtime failure. The reverse source mismatch is a documented source coverage issue under existing InTr and SDK return owners, not justification to generate receipt facsimiles or use KV origin for an SDK reviewer result. Do not require any new physical device or broad Healer cycle. No downstream physical observation or independent LinkedIn completeness can be inferred from these repairs.

**Goal Task state:** preserve ACTIVE; the parent original experiment's separate SDK manifest and source evidence were already completed. The source-repaired generic review path has not delivered this experiment to an evaluator through authentic governed Publisher→SDK return and far-side transition. Next work targets the **existing** reverse SDK reviewer return ingress plus actual preexisting SDK manifest/receipt custody and then checks exact Master Records closure and independent evaluator readback.

## 2026-09-25 bounded immutable-wire digest reconciliation (draft PRs; no runtime claim)

Registry issue #2682 retains ACTIVE/UNCLAIMED / COSV 50000000100000; source audit comment identifies the original-wire vs SDK validation-projection hash mismatch and purpose-worker-only diagnostic ingress mismatch. These are distinct defects. Preserve the frozen original manifest (canonical original wire JSON SHA-256 ad9b8b8aab2beeea04bff2aac34fd2e7bfa5915133bcaef9209c16de7d9bea68, file SHA-256 e1b05a082ce19d3d254e3cde1dced03019174a94287724959672c9e65510c8f3) and original completion.publisher.required=true.

Cross-owner *draft* digest correction: SDK PR https://github.com/StegVerse-org/StegVerse-SDK/pull/323 carries the unchanged original manifest under canonical_manifest plus an independent wire_manifest_sha256 and normalized canonical_manifest_projection bound by the preexisting canonical_manifest_sha256, all included in request_sha256. Central PR https://github.com/StegVerse-Labs/.github/pull/2715 checks independent wire and projection recomputations and retains legacy embedded worker-fixture compatibility. Focused SDK and adversarial central source tests were added. CI exact-head statuses and merges must be independently verified; draft source code is not an authenticated InTr disposition. Source producer validation mismatches must be classified under the producer, not misreported as InTr denial.

**Next distinct source repair under existing owner:** the generic ingress execute() remains a WorkerCoordinator/purpose-worker consumer and refuses ecosystem_diagnostic with canonical_task_id=None. Do not forge a worker claim, assign a dummy task ID or call the local diagnostic processor an authenticated runtime. Reconcile with the already installed SDK ecosystem_diagnostic capability and existing admitted EVENT_EPHEMERAL StegOS/Universal InTr path; implement and test the authentic non-worker graph consumption without adding runtime, host, device, scheduler or authority plane. Each actual attempted transition must return exactly one attributable ALLOW, actionable DENY, or terminal FAIL_CLOSED. DENY may feed existing builder/Healer/LLM Sandbox remediation and a **new governed attempt**; FAIL_CLOSED cannot auto-retry. Include precise predicate, stage, original manifest and packet hashes, task/COSV, existing repair owner, evidence and immediate predecessor references; retain through existing organization custody and reconstruct using corresponding Master Records. Publisher remains required for the frozen original. Do not claim an ephemeral run, organization HEAD verification or genuine Master Records closure before exact authentic retrieval.

## 2026-09-25 merged digest repair and bounded nonworker source disposition

The prior SDK producer PR #323 merged as `f9c2df3718519938590d183dd77998e3a1b8d559` after 14 observed exact-head SDK source workflows passed, including MIR/SV Exp3 manifest #36164510626. The central ingress companion PR #2715 merged as `6b1b6bd4f85d0184b6a90c4ee23b15360fe06b27` after all three observed exact-head central workflows passed, including the focused generic manifest ingress tests in derived lifetime #36164721042. Both changed original-wire/projection digest bindings; **not an authenticated cross-repository runtime test**.

The additional existing-owner nonworker correction branch `fix/mir-exp3-diagnostic-deny-receipt-20260925` replaces the generic purpose-worker-only `canonical_task_id_required` exception for validated null-task diagnostic graphs with a write-once **source/profile DENY** under the existing SDK manifest-state-transition runtime-state directory. It includes exact request/wire/projection hashes, reason `ECOSYSTEM_DIAGNOSTIC_NONWORKER_DISPATCH_UNWIRED`, stage/predicate, original goal/COSV if supplied, existing source owner, expected future evidence and new-attempt condition. This is a bounded diagnostic record only and deliberately reports `authentic_intr_disposition_observed=false` and `organization_master_records_closure_observed=false`. No WorkerCoordinator claim or fake InTr/MR receipt may be fabricated. This branch does **not** establish diagnostic EVENT_EPHEMERAL processing or the later Publisher/far-side closure. Verify branch CI and merge before describing this source-level improvement as installed.

Next source repair: compose existing SDK diagnostic processor and admitted invocation-specific EVENT_EPHEMERAL StegOS consumption through existing Universal InTr, retaining actual original manifest and output bytes, only transition through actual Interlock/InTr dispositions; complete real canonical organization/Master Records predecessor-linked closure and required Publisher/SDK return and far-side review if the frozen manifest requires. Retest original unchanged manifest and continue from genuine per-boundary DENY; stop on authenticated terminal FAIL_CLOSED. Do not conflate returned profile/source DENY with authenticated InTr or Master Records records.


## 2026-09-25 DENY routing and source-repair reconciliation

Canonical task remains `ACTIVE / UNCLAIMED` / COSV `50000000100000` (readback of `data/canonical-task-records/MIR-SV-CAPABILITY-BOUNDARY-EXPERIMENT-003.json`). SDK immutable-wire/projection source PR #323 merged `f9c2df3718519938590d183dd77998e3a1b8d559`; central consumer PR #2715 merged `6b1b6bd4f85d0184b6a90c4ee23b15360fe06b27`. Central diagnostic source/profile DENY PR #2716 is also CLOSED/MERGED (verify exact merge SHA against GitHub); existing installed non-worker diagnostic source profile now returns write-once request-bound `DENY / ECOSYSTEM_DIAGNOSTIC_NONWORKER_DISPATCH_UNWIRED` instead of a misattributed purpose-worker exception. That is not execution of the diagnostic EVENT_EPHEMERAL graph or an authenticated InTr disposition.

Existing StegHealth remediation owner `STEGHEALTH-ECOSYSTEM-FAILURE-REMEDIATION-001` / COSV `10100000100000`, issue StegHealth #51, merged PR https://github.com/StegVerse-Labs/StegHealth/pull/110 as `6d84930ebda648ed14aa7098114c5fe9770f28de` after both exact PR-head source workflows succeeded (36164358304 and 36164358326). This implements source-level reason-code routing: deterministic envelope/hash mismatch -> originating Manifest Builder; custody/replay mismatch -> triggered existing StegHealer; multi-component ambiguity -> existing LLM Sandbox for non-authorizing deliberation; unfamiliar reason -> its existing owning component. A `DENY` carries original manifest/packet hashes, stage, reason, task/COSV and predecessor/receipt references only when such references actually exist. It may prepare a **new** task-governed attempt only after corrective evidence and ordinary admission. `FAIL_CLOSED` remains terminal for the attempted execution and cannot enter this retry path. Reuse the CLOSED StegBrowser Healer correction #2249: no standing Healer prerequisite or new scheduler/runtime/device.

The first original Experiment 3 acceptance fixture is a **producer-level** original-wire missing-embedded-hash mismatch, corrected by the existing dual-digest envelope while keeping the frozen manifest bytes and Publisher requirement. Its source regression is not evidence that this source failure occurred in authenticated InTr. The next installed source/profile disposition for the diagnostic request is `DENY / ECOSYSTEM_DIAGNOSTIC_NONWORKER_DISPATCH_UNWIRED`; route under the existing SDK diagnostic and StegBrowser Universal InTr EVENT_EPHEMERAL integration owners. Existing SDK result consumer must preserve exact returned source/profile DENY separately from completed Master Records state, without relabeling it as authentic InTr or treating an unevaluated attempted transition as terminal failure. Actual completion requires admitted EVENT_EPHEMERAL execution from the unchanged manifest+packet, actual organization HEAD and immediate-predecessor-linked retained receipts, matching Master Records reconstruction, and explicit Publisher return/far-side evaluation as declared by the frozen manifest. No authentic resident execution, organization HEAD, Master Records closure or external physical consequence is claimed from these source-only merges.


## 2026-09-25 reconciling merged digest, exact historical wire and admitted diagnostic consumer

Re-read canonical `data/canonical-task-registry.json` at generation **243** and exact task record: `ACTIVE / UNCLAIMED`, COSV `50000000100000`; no checkout or governed runtime state transition was performed by these source PRs. SDK #323 merged `f9c2df3718519938590d183dd77998e3a1b8d559`, central #2715 merged `6b1b6bd4f85d0184b6a90c4ee23b15360fe06b27`, central #2716 merged `95215cd81999715979e2cccc5a7a64ba44319854`, with the exact previously documented source CI results. SDK #325 merged `6f333bf961b52923778e9dad883d55a5095b0199`; its 14 applicable exact-head source workflows passed, including Exp3 workflow #36168636607. That run reproduced *exactly* the original frozen manifest file SHA-256 `e1b05a082ce19d3d254e3cde1dced03019174a94287724959672c9e65510c8f3` and diagnostic SHA-256 `5acde471f793546a7a457ab11cbe142aeefa6637e747550390f1db213cfaeaea`. Original GitHub artifact #10843328013 was separately downloaded and verified against its original ZIP SHA-256 `911ca6f07622c4bc603ee8acbb544ef7eb4c0c9cf624ff8abaf9fb8dc4254417`. Subsequent SDK builder-added optional metadata flags changed the regenerated digest `e2c4eed5552c7f2fcc7c921f72a916f7c22b81097e7c33bfaffac3fe26a22ddc`; SDK #325 deliberately strips *only those two false review-default fields for reconstructing this named historical original*, retaining them for all newly built manifests. Source regression now pins original canonical JSON root `ad9b8b8aab2beeea04bff2aac34fd2e7bfa5915133bcaef9209c16de7d9bea68` and accepts the existing central source/profile DENY without laundering it into authenticated InTr admission.

Existing-owner source continuation in this branch: `workers/sdk_manifest_diagnostic_admitted_consumer.py` consumes the **existing admitted** EVENT_EPHEMERAL substrate rather than introducing a new host or scheduler. It requires a request-content-addressed locator under the existing SDK manifest-state-transition runtime directory and two exact canonical Master Records receipts: authentic InTr ALLOW for the unchanged manifest/request followed by a verified Node/Interlock/lease/runtime binding receipt with every intermediate immediate predecessor independently reconstructed. An absent or mismatched evidence predicate yields retained actionable source/profile **DENY** with exact reason, request/wire/projection hashes and existing-owner repair reference; it cannot be reported as an InTr denial. After verified admission, it invokes only the already-installed SDK diagnostic CLI within the admitted ephemeral runtime root, checks the exact original file SHA-256, retains real result bytes and submits the resulting `SDK_ECOSYSTEM_DIAGNOSTIC_EXECUTED` transition through existing organization-first canonical Master Records custody. An execution failure after invocation starts yields terminal source-local **FAIL_CLOSED**, never an automatic retry. Source-only fake callbacks return `SOURCE_SIMULATION_ONLY` instead of ALLOW and assert no credential leak. Dedicated test coverage is added to existing native generic InTr workflow. This change neither mints claim/fence for a null-task diagnostic nor fabricates an admitted runtime.

**Critical distinction for live acceptance:** the existing authenticated Native Node/Interlock/InTr owner must expose exact request-bound InTr ALLOW and materialized event-ephemeral runtime-binding Master Records receipt hashes through the existing resident profile into the SDK runtime's read-only locator. The current GitHub/CI execution cannot produce or retrieve those sovereign bytes. Repeating source tests or a header-only TVC authorization-id check does not satisfy this predicate. Once authentic origin receipts arrive, the consumer may return processing-stage `ALLOW` with exact result, organization predecessor hash and Master Records closure, but **NOT complete communication**. The unchanged original manifest still requires actual Publisher evidence packaging, SDK reviewer return and far-side transition; repair/continue from their actual dispositions under existing Publisher, SDK and InTr owners. Until authentic completion, retain Registry ACTIVE/UNCLAIMED and do not claim private ledger readback, runtime, physical consequences or evaluator delivery.

## 2026-09-25 cross-owner type and native runtime-readback continuation

SDK #328 merged as `f8140f2aa2a3209b8be47d91b5659b1b4ae21335` after **14/14 exact-head source workflows passed**, including dedicated Exp3 workflow #36170066700. The SDK now distinguishes exact-bound source-level correctable DENY, terminal local FAIL_CLOSED without automatic retry, and an independently typed **nonterminal** processing-stage ALLOW requiring the frozen original manifest file SHA, exact diagnostic result JSON bytes/digest, org/Master Records hashes and explicit still-required Publisher and far-side transition. Source CI does not independently observe private ledger or real runtime.

Central PR #2720 now includes cross-repository source test `tests/test_mir_sv_exp3_cross_owner_wire_binding.py` and the existing generic SDK InTr workflow fetches the precise merged SDK commit with no GitHub-token execution authority. It validates the **unchanged** original canonical-wire SHA and actual source-level central ingress's first returned `DENY` reason `AUTHENTIC_EVENT_EPHEMERAL_BINDING_LOCATOR_REQUIRED`; this source-only reason is correctable under existing admitted native Node/Interlock/InTr and StegOS owner and is **not** an authenticated InTr rejection. It rejects mutated original payload and confirms no manufactured worker receipt. Real runtime execution cannot advance until the existing resident source exposes the actual InTr admission and Node/Interlock/EVENT_EPHEMERAL runtime binding **canonical Master Records receipt SHA-256** to the request-scoped read-only locator. A token/header alone or historic StegBrowser nonce cannot substitute. Continue under the existing native owner; retain the first authentic ALLOW, DENY or terminal FAIL_CLOSED and its immediate predecessor. Successful diagnostic processing remains nonterminal and must continue via original required Publisher, SDK return and far-side evaluator transition.


## 2026-09-25 exact-head source acceptance: frozen cross-owner replay, admitted diagnostic dispatch and typed DENY repair

The existing MIR/SV Experiment 3 remains **ACTIVE / UNCLAIMED** with COSV `50000000100000`. Canonical registry record is unchanged; no authentic current resident execution, organization HEAD or Master Records readback was observed in these source/CI work sessions.

Merged existing-owner source repairs and evidence:
- SDK PR [#324](https://github.com/StegVerse-org/StegVerse-SDK/pull/324), merge `1652ebda3253cf8bd1e8a9bc8c2996c3c0608497`, exact PR head `f0755c6f997a6fd1de06e7ce9be68bc0af4cb5f4`: applicable historical frozen-manifest cross-repository workflow [36176081828](https://github.com/StegVerse-org/StegVerse-SDK/actions/runs/36176081828) **SUCCESS**. Ten tests passed; original file hash `e1b05a082ce19d3d254e3cde1dced03019174a94287724959672c9e65510c8f3` and canonical wire hash `ad9b8b8aab2beeea04bff2aac34fd2e7bfa5915133bcaef9209c16de7d9bea68` matched the pinned historical generator; current SDK and central source-profile test returned an attributed source-only nonworker DENY, not a counterfeit InTr adjudication.
- Central [#2718](https://github.com/StegVerse-Labs/.github/pull/2718), merge `ef92bf84877215234a514fc50d443403352b553a`, exact-head purpose-derived CI **SUCCESS**. After valid transport, the existing generic profile retains a precise correctable historical manifest-binding source DENY and its immutable request/wire evidence; it does not mint authentic InTr ALLOW.
- Central [#2720](https://github.com/StegVerse-Labs/.github/pull/2720), merge `806726237474f55372eb3fc20636f224a9114ab0`, repaired exact PR head `5fcaf173f720deaba424d9cc8559802a5d673634`: all **three** applicable exact-head workflows succeeded, including Purpose-Bound Worker Derived Lifetime `36176749241`. The prior malformed expected wire SHA in the cross-owner test and stale synthetic predicate expectation were corrected; the existing nonworker consumer and historical hash DENY were composed rather than overwriting either. The source-gated admitted diagnostic consumer now demands authentic original-request InTr ALLOW, predecessor-linked EVENT_EPHEMERAL Node/Interlock/lease/runtime binding and exact canonical Master Records reconstruction **before** invoking the installed diagnostic CLI; a local synthetic test never becomes authentic runtime. When its actual resident evidence locator is absent, it returns write-once correctable **source/profile** `DENY / AUTHENTIC_EVENT_EPHEMERAL_BINDING_LOCATOR_REQUIRED` with no automatic attempt replay. A failure after invocation begins is terminal FAIL_CLOSED; successfully recorded diagnostic processing is nonterminal ALLOW pending required Publisher and far-side return.
- SDK [#326](https://github.com/StegVerse-org/StegVerse-SDK/pull/326), merge `9cf1d69c770ea92048a0883adf6ff0dfa09797db`, tested exact head `11d04c09d3eecbbb3c7efdf6efa43ee3ea12b677`: **16/16** applicable exact-head source workflows succeeded. Exp3 dedicated [36177075581](https://github.com/StegVerse-org/StegVerse-SDK/actions/runs/36177075581) passed 12 focused tests. Reuses original SDK Manifest Builder for precisely attributable manifest-binding DENY, verifies original/rejected request hashes and only submits a distinct, changed envelope through the existing governed endpoint. It retains the DENY when output is unchanged and never retries terminal FAIL_CLOSED. Exact original manifest and Publisher requirement remain unchanged. Existing typed source DENY, terminal local FAIL_CLOSED and nonterminal processing ALLOW are preserved from SDK #325/#328.

**Next real native state transition:** Existing Node/Interlock/InTr and StegOS owners, not GitHub, must supply a request-scoped read-only resident locator containing the independently reconstructable, actual InTr ALLOW and EVENT_EPHEMERAL runtime-binding canonical Master Records receipt hashes for the unchanged original manifest and matching original packet, including immediate predecessors. Then run the installed nonworker diagnostic consumer within that admitted ephemeral runtime, retain exact diagnostic output bytes, return an attributable next **authentic** disposition and verify the matching organization-ledger HEAD/immutable predecessor chain and Master Records reconstruction. Every actual DENY routes to the existing builder, StegHealth/triggered Healer or LLM Sandbox owner as appropriate, requiring independently governed re-entry; FAIL_CLOSED terminates the current attempt. A processing-only ALLOW must continue under the frozen Publisher-required external review/SDK reviewer return/far-side evaluator path. Lack of accessible sovereign resident runtime evidence is a readback limitation, not proof that an attempted transition received FAIL_CLOSED or did not occur. No new runtime, scheduler, ledger, authority, external host or additional user-operated device may be introduced.

## 2026-09-25 exact existing-organization-HEAD readback repair candidate

The existing non-worker consumer previously consumed only an already materialized request-addressed admitted locator, but a code search of central default-branch sources located **no real producer** for that schema, only the consumer and synthetic test fixture. Under this task's existing owner, branch fix/mir-exp3-existing-ledger-locator-readback-20260925 modifies the **same installed consumer** (no new endpoint or ledger) to derive its original expected write-once locator from authentic existing private organization HEAD and the current canonical Master Records interface when the direct locator is absent. It reuses resident-runtime/organization_batch_custody.py::_segment to verify complete immutable predecessor chains and full receipts inventory, resident-runtime/aggregate_repo_transition.py::verify_source to check exact original retained sources, and canonical-state-transition Master Records exact digest reconstruction. It selects only a unique same-request/same-original-wire InTr admission ALLOW and an exact descendant EVENT_EPHEMERAL runtime-binding receipt. Ambiguity, wrong origin, invalid SHA, missing source, broken chain or inaccurate Master Records reconstruction fail closed; an inaccessible private HEAD is not interpreted as a runtime DENY. Test-double reconstruction returns source_simulation_only and cannot mint a production locator. Existing post-locator processing, organization-first custody, required Publisher and far-side boundaries remain unchanged. Source/CI acceptance does not establish real private-ledger access, genuine runtime operation, or terminal completion; verify PR exact-head CI and merge only after owner review.

## 2026-09-25 scoped exact-head review and live-lease admission requirements

Central PR #2742 was rebuilt as a single Experiment 3 commit on the latest main after native review found unrelated owners' commits in the initial branch ancestry. Its intended diff is exactly this handoff, the existing non-worker diagnostic consumer, its focused tests, and README; the canonical Registry and all unrelated concurrent source remain inherited unchanged from main. Generation 250 records this goal ACTIVE / UNCLAIMED with COSV 50000000100000. Current native implementation is a review candidate, not an authorized merge or observed resident execution.

Before the immutable original SDK diagnostic may run, its same-request InTr admission and EVENT_EPHEMERAL binding must reconstruct from actual private organization HEAD and canonical Master Records. A historical LEASE_OPEN receipt or cached request locator cannot establish current execution authority: the consumer now requires an owner-supplied bounded lease_expires_at, an exact current ESRL LeaseMachine LEASE_OPEN snapshot digest in the admitted runtime, a same-identity current StegOS local/relay rendezvous check, and a fresh entire organization HEAD reconstruction excluding post-binding revocation, expiry, FAIL_CLOSED, denial, release and already-executed reuse. No existing native runtime-binding producer for the newly required deadline/snapshot fields was verified in this GitHub-only review; the checked-out existing native owner must prove the actual contract and independently approve the exact PR head before merge. A source-test double never becomes a production locator; missing resident readback stays UNKNOWN_NOT_AUTHENTICALLY_OBSERVED, not an invented authentic InTr DENY or terminal FAIL_CLOSED.

After authorized source merge, obtain original request-bound private resident HEAD and exact InTr/Node/Interlock/runtime/lease predecessor receipts through the already-authorized interface, independently reconstruct canonical Master Records, and invoke the unchanged original manifest only in its currently admitted ephemeral lease. Return the exact real result bytes and first authentic disposition. A genuine DENY requires the existing repair owner and separate governed attempt; an authentic FAIL_CLOSED ends that attempt; a real diagnostic processing ALLOW remains nonterminal until original required Publisher external review, generic SDK reviewer return and independent far-side completion are verified. Never substitute source CI for runtime, GitHub credentials for TV/TVC, another device or another authority plane.

### Verified existing-native source contract discrepancy

A bounded read of current source identified a real existing ESRL snapshot production path in `workers/sv002_observation_esrl_runtime_bridge.py` (initial `PUBLIC_VERIFYING` snapshot, digest and ref) and the following `workers/sv002_intr_materialization_consumer.py` / `workers/sv002_public_profile_lease_resumer.py` path (verified final `LEASE_OPEN` snapshot and digest). Neither this StegVerse-002 path nor the inspected `stegos/ephemeral_runtime_lease.py` `LeaseMachine` snapshot proves that the distinct Experiment 3 production `SDK_ECOSYSTEM_DIAGNOSTIC_EVENT_EPHEMERAL_BOUND` source receipt carries an owner-authorized `lease_expires_at`, its exact final snapshot or current revocation semantics. The live native relay verifies runtime identity, not governance admission or non-revocation. The existing Exp3 consumer deliberately requires an MR-closed original request-bound deadline and exact lease snapshot; without them it returns correctable pre-execution source-profile non-ALLOW instead of manufacturing runtime authority. The existing native owner must resolve the producer/consumer contract, with verifiable UTC deadline origin and actual same-invocation source/Master Records custody, before independent source approval and merge. No new runtime or second device is proposed.
