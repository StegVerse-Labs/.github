# SDK Micro-Node Commit-Time Admissibility Mirror Handoff

Updated: 2026-09-22
Goal Task ID: `SDK-MICRO-NODE-COMMIT-TIME-ADMISSIBILITY-001`
Issue: https://github.com/StegVerse-Labs/.github/issues/2585
COSV: `71000000111111` (SDK experiment task-family vector; canonical Task Registry generation 203; source CI five applicable checks green)
Status: ACTIVE / HANDOFF_READY — registered on canonical main; no execution claim or runtime authority

## Narrow owner
A new independently-attributable multi-worker micro-node commit-time admissibility experiment. Reuse the existing SDK generic manifested worker evaluation and micro-node runtime; existing CTA standing formalism, StegCore/Universal InTr admission, TV/TVC warrant, WorkerCoordinator/StegAgents lifecycle, canonical organization ledger and Master Records. Do not create second runtime/scheduler/dispatcher/claim authority/credential/custody store/device prerequisite. Prior work remains immutable.

## Collision and compatibility
- ARCHITECTURE-NEUTRAL-ADMISSIBILITY-001: RETIRED bounded formalism, explicitly no mandatory GTG integration.
- SDK-FOUR-STAGE-EVIDENCE-REMEDIATION-001: RETIRED exact-head locally reproducible disjoint partition/reconstruction, recomputed group bindings, measured post-barrier overlap with serialized negative control. No authentic governed execution inferred.
- SDK-TT-RICHARD-SEAM-AUTHENTIC-RUNTIME-001: ACTIVE/CHECKED_OUT; owns genuine single short-lived task/worker ACTIVATE+CREATE_AND_BIND, close/retire and records-only reconstruction. This experiment must consume its authoritative lifecycle proof and not duplicate source repair.
- Admissible-Existence/CTA: current standing required at commit time, not prior approval.
- Architecture-neutrality is not ALLOW; independent workers agreeing is not ALLOW.

## Pre-registered experiment
Freeze exact manifested transition, input/policy/current-state hashes, source revision, worker topology and partition specification before executing.
1. At least three separately attributable micro-node workers evaluate the same proposed consequence-bearing transition, preserving per-worker authority, observation scope, evidence, disagreement and explicit UNKNOWN.
2. Prove actual independent evaluation/custody boundaries, or classify mere replica/correlated evidence as such. For partitioning, require disjoint coverage and deterministic reassembly.
3. Measure true post-invocation overlap against serialized negative control, without making positive overlap mandatory for a short workload.
4. Existing CTA/StegCore/Universal InTr evaluates *current* commit standing with valid TV/TVC warrant; no quorum/majority automatically grants ALLOW. Missing mandatory evidence fails closed or escalates under applicable policy.
5. For each successor transition reconstruct the exact immediately preceding canonical Master Records receipt at emission. Require state=RECORDED, reconstruction_status=PASS, required_evidence_validation_status=PASS and receipt_sha256=reconstructed_receipt_sha256.
6. Expire workers, preserve individual dissent, and independently reconstruct worker results, aggregate observation and governed decision from retained records alone.

## Falsification
Unanimous workers sharing missing evidence; independently conflicting witnesses; stale policy or warrant between review and commitment; omitted partition; forged predecessor / mismatched hash; invocation before constitutive closure; expired worker re-invocation; submitter removed before replay. No source-only, GitHub Actions or simulated result promoted to authentic runtime observation.

## Execution order
Reconcile the existing CTA source contract, SDK public manifests, generic worker partitioning/group binding and micro-node construction, canonical organization ledger, Universal InTr admission and live Master Records custody. Check the ACTIVE Richard seam task for authoritative prerequisite evidence before any governed invocation. Add only missing generic compatibility code and reproducible local fixtures, then collect authentic retained runtime observations on the existing worker path. Update README and this handoff in each substantive source PR.

## External site
Dan Gray's https://collapse-resolution.com/ is context from a shared public post. Website content could not be fetched in this session; it is not an external coauthor, evaluator, authority or source of experimental proof.

## Current evidence
Canonical Task Registry generation 203 registered by PR #2586 / squash merge 2371cbf551ab526fd89d3b0c1abc17899bda6c7e; five applicable exact-head workflows passed. This closeout projects the validated registration to generation 204. ACTIVE/HANDOFF_READY is coordination only: no WorkerCoordinator claim/fence, governed execution, externally independent custody, live multi-worker runtime receipt or reconstruction is claimed.


## SDK and micro-node contract reconciliation — 2026-09-22

Registration verified on canonical main at generation 204: PR #2586 registration squash merge `2371cbf551ab526fd89d3b0c1abc17899bda6c7e`, subsequent coordination projection `80674f0b01b23c1a9c871798018644a3cef96104`. The branch from PR #2586 is behind main and requires no second merge. Canonical record remains ACTIVE/HANDOFF_READY with COSV `71000000111111`. Prior handoff records five applicable exact-head checks green, but this reconciliation does not independently reproduce those workflow logs or claim runtime proof.

### Verified source interfaces and first compatibility gaps

1. SDK `stegverse/purpose_bound_worker_processor.py` publishes `stegverse.sdk.purpose-bound-worker-group-test.v1` with 2–32 workers, distinct `partition_ids`, a normalized per-worker cost/lifetime policy, and `stegverse_purpose_bound_worker_request` extension. It deterministically partitions source text into disjoint contiguous ranges, checks ordered exact reconstruction, binds each worker result to a recomputed group commitment, and measures genuine post-invocation intervals against a serialized negative control. SDK `tests/test_four_stage_manifest_experiment.py` asserts these local semantics. The published purpose-bound worker and atomic-task-worker routes are `SDK_LOCAL_SEMANTIC_DEMONSTRATION`, not authentic governed runtime. **Gap:** local semantic group outputs do not establish independently originated observations, current CTA standing, InTr admission or authentic resident execution.
2. The micro-node runtime `schemas/micro_node_request.schema.json` requires `transition_id`, `origin_system`, `return_path`, `action`, `actor`, `target`, `scope`, and optionally accepts `payload`, `policy_ref`, `delegation_ref`. Its current `schemas/micro_node_receipt.schema.json` binds `receipt_id`, `receipt_hash`, `transition_id`, `request_hash`, `decision`, `role_evidence_hash`, `previous_receipt_hash`; its decision schema contains eight or more role results and governed return/reconstruction. **Gap:** none of these schemas, by themselves, encode the exact SDK group-manifest identity, separately verifiable per-worker authority and independent observation origins, explicit UNKNOWN/dissent, current CTA standing proof, or canonical Master Records predecessor reconstruction. Do not infer these from a generic micro-node receipt.
3. Public CTA formalism requires actual present standing at the commitment boundary, distinguishing prior review and current commitment. CTA documents `ALLOW`, `DENY`, `FAIL_CLOSED`, `ESCALATE` and `REFUSE`; the micro-node decision/receipt schemas currently enumerate only `ALLOW`, `DENY`, `FAIL_CLOSED`. **Compatibility requirement:** preserve source CTA/InTr disposition faithfully in the existing governed decision/receipt envelope; do not silently coerce ESCALATE or REFUSE into ALLOW or a micro-node-issued authorization. A bounded projection into the micro-node tri-state surface may be defined only with an explicit reason and retained original disposition.
4. Existing authentic Test-3 Richard seam owns WorkerCoordinator claim/fence → TV/TVC → InTr atomic ACTIVATE+CREATE_AND_BIND → canonical Master Records closure → worker invocation/result → InTr close/retire → records-only reconstruction. This proposed group experiment must consume, not fork or bypass, that lifecycle. Validate each worker's own exact predecessor at emission; generic `previous_receipt_hash` presence is not predecessor reconstruction or authoritative custody.

### Bounded next source work

Use the public SDK Manifest Builder/run-manifest route to generate one frozen three-worker group experiment with explicit independent-origin annotations and evidence limitations in an experiment-specific contract (not undocumented required SDK fields). Establish an adapter through the *existing* micro-node request/runtime path that binds the exact manifest hash, per-worker output hashes, original evidence sources, retained disagreements/UNKNOWN, worker claim/fence identities, and current CTA/InTr decision; preserve each existing schema's authority boundary. Before adding any source field, prove whether existing SDK extensions and existing canonical receipts already carry it. Test deliberate identical-output/shared-missing-evidence, stale standing and missing partition fixtures without synthesizing external authority. Distinguish source/CI fixtures from genuinely independent custody and authentic governed runtime. No additional runtime, scheduler, WorkerCoordinator, authority plane, custody database or connected device.

This reconciliation is documentary/source-interface inspection only. No SDK or micro-node schema or runtime has been changed, and none of the experimental authentic runtime predicates is satisfied by it.


## Three-worker SDK source contract merged — 2026-09-22

Canonical registration stays `ACTIVE / HANDOFF_READY` at observed Task Registry generation 204, COSV `71000000111111`. SDK source PR [#309](https://github.com/StegVerse-org/StegVerse-SDK/pull/309) merged at `78100556a5b8e0314dc3dc18ff3078df8a0487d8`; tested exact head `223dd454ce413136d6521cfb2c197007ec89b3bd` completed twelve PR-triggered workflows successfully, including targeted `tests/test_micro_node_commit_time_experiment.py` through the existing Manifest Builder Source Validation workflow (run `35817502084`). The SDK README and SDK_MIRROR_HANDOFF.md were updated. No Task Registry transition or worker checkout was performed by this source merge.

The non-authorizing `stegverse/micro_node_commit_time_experiment.py` consumes the existing three-worker purpose-bound group manifest and grouped result plus an experiment evidence descriptor. It reuses existing SDK partition derivation and group binding, and checks the **existing** micro-node request envelope shape with exact manifest/group, transition and policy identity. Per-worker descriptions preserve worker/result attribution, declared source/custodian, KNOWN/UNKNOWN/DISSENT, mandatory evidence, current CTA standing and original CTA disposition, TV/TVC warrant claims, and exact predecessor/reconstructed receipt hash claims. It returns `NON_AUTHORIZING_LOCAL_REVIEW_ONLY` even when local fixtures claim everything passed. Positive/negative source tests cover shared missing evidence, correlated origins, dissent/UNKNOWN, CTA DENY/FAIL_CLOSED/ESCALATE/REFUSE, stale standing, forged predecessor, omitted partition, tampered binding, changed manifest, mismatched micro-node group hash and policy reference. GitHub CI is **source/semantic evidence only**; independence, actual CTA/InTr decision, TV/TVC verification, authentic custody, worker retirement and records-only replay remain UNKNOWN_NOT_AUTHENTICALLY_OBSERVED.

Next boundary: materialize authentic group worker receipt inputs through existing SDK and micro-node runtime and the separately owned Richard single-worker constitutive lifecycle; prove source origin independence and per-worker canonical immediate predecessor at emission. Do not extend the local semantic result into a fabricated external ALLOW or synthesize a generic micro-node receipt as Master Records proof.

## Separation-of-powers experimental sequence — proposal reconciled 2026-09-23

**Coordination:** Existing canonical Goal Task `SDK-MICRO-NODE-COMMIT-TIME-ADMISSIBILITY-001` owns the bounded multi-worker admissibility experiment (ACTIVE/HANDOFF_READY; COSV `71000000111111`). Existing `SDK-TT-RICHARD-SEAM-AUTHENTIC-RUNTIME-001` owns short-lived worker lifecycle, not a second experiment runtime. This proposal changes experimental ordering and evidence requirements, not Task Registry coordination state, checkout or authorization. Richard's external participation has **not** been confirmed by a jointly ratified protocol; the user reports sending the proposal, which is not proof of acceptance. Stage 1 can proceed locally with clearly labeled simulated external inputs; authentic cross-framework conclusions require a participating external framework and independent retained evidence.

### Stage 1 — capability discovery and conditional co-development

Start with the proposed manifold-style experiment to establish **demonstrated**, rather than self-declared, capabilities and compatibility. Pre-register exact participant identities and roles, manifest/source hashes, observation scope, provenance/custody boundaries, allowed information exchange and evaluation criteria. Each participant independently attempts the same bounded observation and reconstruction tasks. Preserve source-level claims, what was actually measured, claimed blind spots, dissent, uncertainty, and failure to observe. Exercise deliberately conflicting observations, correlated sources falsely presented as independent, shared missing evidence, missing records and participant-absent replay. Compare individual vs jointly reconstructable results **without** promoting agreement to independent proof. Report a capability/compatibility matrix specifying demonstrated, unsupported, contradictory and untested interfaces. A declared unknown is not an empirical estimate of all undetectable events. Stage 1 tests whether interoperation and possible co-development are feasible; it is not a prerequisite to continuing StegVerse core development.

### Stage 2 — independently supplied work, governed transition, independent measurement

Stage 2 may involve a **different third-party framework** from Stage 1. An external framework supplies data, proposed action and/or execution on an existing SDK manifest ingress path; it retains origin and execution authority over its own system. The existing CTA / StegCore / Universal InTr chain independently assesses current commit-time admissibility using TV/TVC authority; it does not infer ALLOW from agreement or from third-party execution. A logically and custodially independent measurement role captures the affected system's observable baseline **before** the decision and independently measures the outcome **after** the decision/attempt. Observer independence must be demonstrated, not assumed from a distinct worker ID. Source framework, governance plane and observer have distinct attribution, evidence, credentials and scope; no extra governance authority is created.

Pre-register the *actual-state measurement* contract: baseline observable variables, measurement procedure and expected sensitivity, external observer identity/custodian, synchronized event/commit correlation, timing and uncertainty bounds, intended mutation and expected consequences, source manifest/policy/warrant identifiers, stop conditions and known excluded observables. Bind pre-commit baseline and external proposal to the frozen manifest; retain the actual commit-time InTr disposition and predicates (including DENY/FAIL_CLOSED/ESCALATE/REFUSE), authorized actuation or non-actuation, externally supplied execution report where relevant, and independently measured post-commit state. For denied or noncommitted cases, observe whether unintended mutations nevertheless occurred. Compare: initial state vs final observed state; submitted inputs vs admissibility evaluation; authorized consequences vs external execution report; execution report and governance receipts vs independently measured actual outcome. Classify each residual as a measured discrepancy, attributable unrecorded transition, known measurement/recording gap, bounded uncertainty or unresolved/unobservable under the declared measurement limits. A zero detected residual is **not** proof of complete observability, and receipt closure is **not** proof of the external outcome.

**Entry from Stage 1:** independently retained demonstrations establish compatible manifest transport, claim/evidence attribution, preservation of disagreements and unknowns, an observation contract and worker-absent records-only reconstruction within their tested scopes. A third-party framework may be substituted for Stage 2 only if its own source/authority and interchange interfaces are proven. No participant commitment or successful interoperability is assumed.

**Evidence and falsification:** Use existing SDK/micro-node/WorkerCoordinator/TV/TVC/InTr/org ledger/Master Records surfaces only. Require verified current standing and immediate canonical predecessor at each emission, independent pre/post custody and exact source/result/decision correlations; preserve per-worker UNKNOWN and dissent. Negative controls: fabricated participant capabilities, common-source correlation, withheld baseline, silent post-commit mutation after DENY, ALLOW with no actual actuation, an execution report contradicted by observation, timing skew, shared-observer failure and valid receipts that cannot explain an independently observed consequence. Source fixtures may exercise these semantics but never count as authentic governed execution or independent external observation. Authentic proof is still UNKNOWN_NOT_AUTHENTICALLY_OBSERVED.

**Next development boundary:** validate this experimental order with relevant participants, map the Stage 1 evidence descriptor to existing SDK extension fields, then implement only missing compatibility and independent observer evidence carriage on the *existing* paths. Update canonical Task Registry predicates only through its generation/COSV-governed transition process, not via this documentary PR. Preserve all prior handoff assertions and prior successful source work.


### Stage 1 interface map against existing SDK and short-lived Richard seam — source inspection 2026-09-23

The merged SDK `stegverse/micro_node_commit_time_experiment.py` already validates the ingress manifest with `validate_ingress_manifest`, reads the existing `stegverse_purpose_bound_worker_request` extension and checks exactly three workers, distinct partition bindings, canonical result/group commitments, `records_only` and worker expiry. Its separately supplied `stegverse.sdk.micro-node-commit-time-experiment.v1` evidence descriptor binds exact manifest hash, transition/policy identity, `micro_node_request.payload.sdk_manifest_sha256`, group hash, per-worker result hashes and declared `source_id`, `custodian_id`, `observation` (KNOWN / UNKNOWN / DISSENT), mandatory-evidence presence, purported independence, predecessor custody, standing and original CTA disposition. It returns `NON_AUTHORIZING_LOCAL_REVIEW_ONLY` even if all caller-supplied assertions are positive. Three unique source/custodian strings and `independence_verified=true` are **not** proof of independent provenance; source-local review must never become Stage 1 external capability proof.

Reuse the manifest's existing extension/evidence descriptor rather than extending a public mandatory schema for this documentary stage. Stage 1 record requirements map as follows: (a) preregistration: immutable manifest/group/policy/participant-role identifiers and source revisions in the existing evidence packet; (b) each observation: exact worker/partition/result binding, actual source origin and external custodian evidence if present, measured scope/uncertainty and explicit UNKNOWN or dissent; (c) capability tests: attributable ingest and output, falsifiable observation, contradiction/correlated-input controls, missing mandatory evidence, and submitter/worker-absent records-only reconstruction; (d) compatibility conclusion: explicit PER_CAPABILITY status of DEMONSTRATED / FAILED / UNTESTED / CORRELATED_ONLY with retained source/custody references and measurement limits. These Stage 1 capabilities must be independently witnessed before an external framework is declared compatible.

The separate canonical `SDK-TT-RICHARD-SEAM-AUTHENTIC-RUNTIME-001` remains ACTIVE/CHECKED_OUT, COSV `20010000110000`; its Test 3 owns `HANDOFF_READY` -> fresh WorkerCoordinator claim/fence -> warranted InTr atomic ACTIVATE+CREATE_AND_BIND -> canonical Master Records closure -> invocation/result -> governed CLOSE+RETIRE -> executor-absent replay. Consume its authentic task/worker lifecycle receipts where available; neither its source-pass fixture nor the three-worker local evidence descriptor provides authentic external observations. Do not invent a runtime gate requiring a device or pre-existing idle runtime. At actual transitions, inspect existing organization receipts for the first failure, remediate in the owning seam and retest rather than promoting synthetic assertions.

**Stage 1 exit:** retain an evidence-backed compatibility report that distinguishes local simulated fixture checks from authentic cross-framework tests. External cooperation is optional for StegVerse independent development; Stage 2 with a different external framework requires that framework's own attribution and manifest interoperability demonstration. No local review may issue CTA/InTr ALLOW, satisfy WorkerCoordinator claim/fence, or claim Master Records custody.


## Stage-1 SDK capability descriptor merged — 2026-09-23

SDK PR [#312](https://github.com/StegVerse-org/StegVerse-SDK/pull/312) merged as `f7baa8aa17feb82b4a4b3dfbb876f738ddce1a8c` after twelve exact-head PR workflows reported SUCCESS, including the Manifest Builder Source Validation workflow running the expanded `tests/test_micro_node_commit_time_experiment.py`. The SDK added `stegverse/stage1_capability_discovery.py`, a strictly non-authorizing post-validator of the existing three-worker manifest/group/evidence. Separate descriptor `stegverse.sdk.stage1-capability-discovery.v1` preserves per-participant exact manifest/group/worker/result origin and custodian bindings, observation packet hash, required observation limits, KNOWN/UNKNOWN/DISSENT and source-level INGEST/OBSERVE/RECONSTRUCT claims. Positive and negative fixtures cover unique-but-unverified origin claims, shared observation packets, shared custodians, missing/altered identities, substituted manifest, retained unknown/dissent, omitted observation limits, unsupported claims incorrectly carrying evidence and mismatched packet binding. Source validation is not external witness proof.

The SDK-local return is always `NON_AUTHORIZING_LOCAL_REVIEW_ONLY`, with `external_participation_proven=false`, `independent_origin_proven=false`, `authentic_governance_proven=false`, `master_records_custody_proven=false` and `authority_effect=NONE`. Its `LOCALLY_CLAIMED` capability labels mean only syntactically bound caller assertions; even a consistent fixture demonstrates no real external ingestion, independent observation, custody or reconstruction. External participation remains UNCONFIRMED. Existing `SDK-TT-RICHARD-SEAM-AUTHENTIC-RUNTIME-001` is the separate ACTIVE/CHECKED_OUT lifecycle owner and retains WorkerCoordinator/TV/TVC/InTr/Master Records proof responsibilities. Stage 2 external data plus independently measured pre/post-transition consequences remains a proposed later experiment. Task Registry remains ACTIVE/HANDOFF_READY; this documentation adds no checkout, new runtime, device or authority.
