# KV-Bound Ephemeral Browser Projection Mirror Handoff

Updated: 2026-09-14

Goal Task ID: `KV-BOUND-EPHEMERAL-BROWSER-PROJECTION-001`
Parent Goal: `GLOBAL-RUNTIME-EVIDENCE-MEASUREMENT-001`
Root Goal: `GLOBAL-RUNTIME-EVIDENCE-CLOSURE-001`
COSV: `50000010100000`
Canonical issue: `StegVerse-Labs/.github#1299`
Status: `ACTIVE / AUTHENTIC G7 FENCE7 RETAINED / DEVICE-BOUND CONTINUATION SUPERSEDED / PROVIDER-NEUTRAL ANY-DEVICE REBIND REQUIRED`

## Mandatory canonical device policy

Read and apply before any local interpretation:

```text
control/device-replaceability-invariant.json
docs/DEVICE_REPLACEABILITY_INVARIANT_MIRROR_HANDOFF.md
```

The user-operated device is an interchangeable access/transport endpoint. No particular iPhone, browser, OS, Safari session, service worker, IndexedDB instance, or browser-local state may become the continuity root or required runtime-completion surface.

KV/MyKV provider identity is independent of client-device identity. Google Drive, iCloud, and other configured providers are accessed through provider-neutral KV semantics.

Any `CURRENT_IPHONE_*`, `current-iphone-*`, `same-device-*`, `CURRENT_DEVICE_*`, or `ESTABLISHED_CURRENT_IPHONE` strings in historical evidence, file names, symbols, or old predicates are `NON_NORMATIVE_LEGACY_LABELS_ONLY`.

## Reusable Task Component Model

The Goal Task remains `KV-BOUND-EPHEMERAL-BROWSER-PROJECTION-001`; COSV remains `50000010100000`.

Canonical authority model:

- Task Registry: coordination only.
- WorkerCoordinator: claim/fence authority.
- Interlock/InTr: governed transition/admission authority.
- TV/TVC: credential/provider/release authority.
- KV/SKAP Vault: user continuity/verification boundary.
- StegOS/user devices: interchangeable access/transport/execution endpoints only.
- Master Records: observed-reality custody and reconstruction authority.
- HeartBeat: observability only.
- GitHub: source/evidence coordination only.

## Retained authentic evidence

TASK-2026-0011 generation 7 / fence 7 remains authentic with `ALLOCATION_COMPLETE`, `CLAIM_GRANT_OBSERVED`, and journal replay PASS.

Historical provenance may say that evidence was observed on a current iPhone. That identifies the observation location only. It does not bind future continuation to that device and does not authorize replay solely because the user changes devices.

Previously merged Site, TVC, and StegOS source remains historical/source evidence. Device-specific source names do not define current architecture.

## Superseded implementation path

The prior implementation path used labels such as:

```text
CURRENT_IPHONE_TESTFLIGHT_SIGNING
current-iPhone bootstrap
same-device recovery
current-iPhone TVC surface
```

Those labels are superseded as normative architecture.

The canonical continuation is:

```text
retained TASK-2026-0011 G7/fence7 evidence
-> provider-neutral MyKV/KV reconstruction from configured provider(s)
-> any authorized user-device endpoint only where interaction is needed
-> Interlock/InTr admission
-> bounded ephemeral signing/execution with no device-unique continuity
-> TV/TVC provider/credential execution
-> TestFlight observation
-> retained StegOS/StegBrowser runtime observation
-> Master Records reconstruction
-> return to the frozen global runtime measurement
```

## Current first unresolved predicate

Canonical predicate:

```text
TESTFLIGHT_AUTHORIZED_USER_DEVICE_RUNTIME_OBSERVED
```

Legacy alias retained only for historical lookup:

```text
TESTFLIGHT_CURRENT_IPHONE_RUNTIME_OBSERVED
```

The legacy alias MUST NOT be interpreted as requiring an iPhone.

## Current exact implementation gap

The most recent legacy-path observation identified:

```text
current_iphone_testflight_bootstrap:current_iphone_signing:Load failed
```

Do not patch that failure as though the iPhone were the required runtime owner. First rebind the signing/TestFlight continuation to provider-neutral KV reconstruction and device-replaceable execution semantics. Reuse the existing WorkerCoordinator, Interlock/InTr, TV/TVC, KV, and Master Records owners.

Do not create another runtime owner, scheduler, dispatcher, credential route, provider plane, same-device retry framework, or browser-local continuity store.

## Prohibited guidance

Do not instruct the user to:

- keep using one particular iPhone;
- preserve one Safari session or IndexedDB/service-worker controller for continuity;
- avoid switching devices as an architectural requirement;
- use iOS Files as the canonical path to a Google Drive-hosted MyKV;
- replay G7/fence7 merely because the device changes.

A temporary evidence-preservation instruction during one bounded observation does not create continuity ownership.

## Manual work

None until the machine-side provider-neutral/device-replaceable rebind is complete.
