# StegBrowser A1 Observer Validation Trigger

This source-only marker causes repository validation to run against the exact main snapshot containing the StegBrowser A1 resident request, live Universal InTr profile observer, resolver binding, and deterministic tests.

Authority effect: `NONE_SOURCE_VALIDATION_ONLY`.

It does not prove A1, A2, A3, A4, runtime execution, WorkerCoordinator claim/fence, Interlock/InTr admission, or Round Trip 1 processing.
