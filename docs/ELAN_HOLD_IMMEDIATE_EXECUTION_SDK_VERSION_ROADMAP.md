# ÉLAN HOLD immediate delivery — experiment-specific plan

**Scope:** Only the already-authorized `ELAN-PAPER-COAUTHOR-PUBLICATION-001` experiment and its independently collected source evidence. **The StegVerse SDK, Manifest Builder and Publisher version roadmap is evaluator-independent** and is now maintained in [the ecosystem capability roadmap](ECOSYSTEM_SDK_MANIFEST_PUBLISHER_VERSION_ROADMAP.md). No particular evaluator, research project, publication venue or deadline sets general release priorities.

## Time-critical parallel experiment steps

1. **Native source capture:** Send the existing [chronological evaluator packet](ELAN_HOLD_CHRONOLOGICAL_EVALUATOR_PACKET.md) to the coauthor for exact prompt, API/session and retention agreement. Collect genuine source-native invoked responses, error/nonresult metadata and a client-only no-request control. Native collection can proceed immediately after joint experimental agreement; it does not await a future SDK release.
2. **Current SDK path:** The generic source-observation preflight merged with SDK PR #340 and passed source validation. With actual source bytes, use the installed public Manifest Builder, requested existing processor/route and generic return projection. Record the actual first route disposition or precise failed transition. Preflight `READY_FOR_MANIFEST` is not InTr `ALLOW`.
3. **Evidence and presentation:** Preserve all original private source bytes and hashes. Attempt the applicable original admission, complete predecessor chain, Master Records custody, replay and reconstruction through existing authorized owners. If review-facing output is declared, reuse Publisher's existing generic reviewer package and verify only stages actually observed. Never infer a terminal far-side transition from source or local CI.
4. **Scientific/publication review:** Revise the coauthored manuscript and existing FAccT abstract from genuine experimental observations only. Coauthor agreement to exact protocol is separate from final paper or submission approval.

## Exact current experiment status (September 25, 2026)

- Historical Experiment 2 source correction and manuscript Revision 3 are merged. Confidential original/corrected PDFs were independently compared; no model output occurred during the original no-invocation interval.
- Proposed chronological HOLD series is drafted, **not jointly frozen**, and no new authentic provider-result block has been received.
- Generic SDK source guard is merged with validated source tests. It is on the moving development line, not an independently published runtime release.
- This exact HOLD experiment still lacks independently observed invoked source, applicable genuine governed disposition, complete required custody/reconstruction and any declared Publisher/far-side completion. No synthetic demonstration may satisfy those predicates.
- The generic roadmap's SDK 1.4, 1.5, 1.6 and component compatibility plans are **not owned or gated by HOLD**. Any general-purpose capability discovered by this experiment is referred to existing native owners and the canonical Task Registry; the experiment proceeds with the currently supported path and records exact unsupported requirements rather than waiting indefinitely.

## Immediate successor

Agree the exact chronological stimuli and recording with the coauthor, receive the first authentic invoked result block, submit the unchanged source through the existing generic SDK path, and report the first authentic transition disposition with its original evidence. Preserve separate scientific review and final submission authorization.
