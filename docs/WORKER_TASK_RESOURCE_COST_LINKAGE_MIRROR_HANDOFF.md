# Task-native Worker Resource-Cost Linkage — Preregistration Handoff

**Goal proposal:** `WORKER-TASK-RESOURCE-COST-LINKAGE-001`  
**Central intake:** [StegVerse-Labs/.github issue #2619](https://github.com/StegVerse-Labs/.github/issues/2619)  
**Registration state:** DRAFT SOURCE PR ONLY. Latest inspected registry generation 212. The branch proposes generation 213; **rebase and rederive if main advances**.  
**COSV:** NOT DERIVED; derive from the canonical admitted registry state only after authentic gate and owner reconciliation.  
**WorkerCoordinator claim/fence:** NONE. **Runtime custody:** UNKNOWN.

## Why this preregistration exists

The existing `scripts/evaluate_task_registry_collision_checkin.py` returns `STOP_NOT_REGISTERED` for an ID absent from the canonical Registry. An issue cannot substitute for a registered task. The established central pattern is a coordination-only source PR (row, exact shard, handoff, README); after exact-head validation and merge, re-read the exact current Registry generation and invoke the real `scripts/evaluate_task_registry_ai_session_checkin.py` via its **authorized** AI_SESSION_GATE session origin. Never fabricate a session ID or claim source execution as an authentic runtime receipt.

## First boundary and collision reconciliation

Candidate component scope is additive task-native cost linkage and SDK manifest acceptance. The existing purpose-bound worker model/StegAgents owner retains lifetime semantics, WorkerCoordinator retains assignment and claim/fence, and `StegVerse-org/StegVerse-SDK` owns its existing Manifest Builder, purpose_bound_worker processor and published route. The retired original proof and Test1 successor remain retired. `ORGANIZATION-BATCH-CUSTODY-REPLAY-001` ACTIVE/HANDOFF_READY COSV 10000000100000 owns organization receipt replay, worker-expiry batch closure and actual receipt-cost measurement, **not** the five-part task-native estimate.

Component-010 source shard `data/canonical-task-records/ECOSYSTEM-INGRESS-AI-BOUNDARIES-001.json` declares ACTIVE/CHECKED_OUT but remains absent from generation 212 monolithic registry. Existing `scripts/install_and_run_canonical_work_event_bootstrap.py:refresh_registry_projection_from_shard` supports only exact-shard, idempotent projection repair without new identity or authority. The checked-out owner must reconcile this separately; this preregistration PR does **not** modify that task or assert an authentic component-010 session.

## Integration after authentic admission

Reuse the five-component `DERIVED_COST_TASK_DELAY_BUDGET` (expected_task_execution, known_delay, inferred_unknown_delay_reserve, records_decomposition, safety_reserve). Require exact task ID and specimen/scope digest, nonnegative integer seconds inputs, claimed sum equality, existing class cost-basis reference and declared estimate provenance. Incomplete, dangling, stale or mismatched values fail closed. Explicitly require source evidence for seconds-to-heartbeat conversion. Completed-heartbeat P90 estimators, source-bounded bootstrap HB budgets and task-native seconds are distinct; provisional sample_count=0 estimates cannot become empirical proof. No global default, new scheduler, dispatcher, ledger, WorkerCoordinator, authority plane or device prerequisite.

## Mandatory SDK acceptance

After existing SDK owner admission, pass the exact source specimen through `stegverse.manifest_builder.build_manifest(process="purpose_bound_worker")`, the installed published processor and actual SDK manifest execution. Positive and negative tests must verify round-trip task resource-cost linkage, five-part derivation, exact manifest/request/result digest lineage, malformed/missing/mismatched cost rejections, no authority expansion and records-only retirement. The current SDK's local semantic worker route is **not** an authentic governed WorkerCoordinator/InTr/Master Records invocation; verify same-invocation claim/fence and TV/TVC, applicable InTr, org and Master Records receipts independently before promoting runtime status. Canonical CI must pass on the exact SDK PR head.

## Receipt-cost coordination

`ORGANIZATION-BATCH-CUSTODY-REPLAY-001` remains the canonical owner of actual per-transition receipt bytes/count, local replay duration, required evidence bytes, expiry-triggered batch closure cost, Master Records submit/ack latency and retries. These measurements remain UNKNOWN until authentic observed source events. Where immediate Master Records closure is required by a transition contract, local queueing does not waive it.

## Current evidence / nonclaims

Initial candidate standalone validator: 24/24 local tests reported passing in the source review; the six actual-SDK-dependent tests were skipped in the earlier environment. These historical results are not CI on this PR, installed SDK integration or authentic runtime evidence. No authentic check-in disposition/event was observable through GitHub issue/comments/default-branch files as of 2026-09-24.

**First next executable transition:** merge this reviewed coordination-only preregistration on exact-head CI; re-read current generation; obtain the existing AI_SESSION_GATE owner's authentic generation-bound check-in event and collision disposition; apply only separately admitted component-owned source changes.
