# Joint ÉLAN × StegVerse HOLD Experiment — proposed preregistration (author review)

**Status:** PROPOSED / NOT EXECUTED / JOINT DESIGN PENDING  
**Parent:** ELAN-PAPER-COAUTHOR-PUBLICATION-001  
**Date:** 2026-09-23  
**Purpose:** Compare native outputs to actual silence-related API prompts without treating no invocation as ÉLAN behavior.

## Evidentiary correction motivating this proposal

The previously returned Test 2 PDF included an experimenter-authored silence annotation in an ÉLAN-response field. The coauthor confirmed on 2026-09-23 that this was a labeling correction discovered during transcript review; **no reproduction attempt occurred**. Preserve the original and corrected source documents with version identifiers, but exclude the annotation from all model-generated-output analyses. No-request intervals evidence only experimenter timing/absence of invocation. The original controlled StegVerse A3/A4 governance/custody outcomes concern supplied representations and do not independently prove an ÉLAN-native state comparison.

## Independent conditions

1. **No-invocation control:** start/end timestamps at the client; submit no API request during the interval; record the absence of invocation only. Expected evidence is a client-side event log, **not** an ÉLAN response, state or choice.
2. **Explicit HOLD:** submit a jointly agreed verbatim silence instruction; preserve native request/response, request and response timestamps, API metadata normally exposed, timeout/error information, and whether output was empty, text (e.g. ellipsis), or refusal. Do not preclassify ellipsis as an internal decision.
3. **Direct silence question:** submit a verbatim question about silence without instructing silence; capture the same native data.
4. **Ordinary-response control:** submit a context-matched neutral message and preserve the same observables.
5. **Return-to-speech:** use a preregistered verbatim return prompt after the respective eligible invoked conditions; record exact sequence timing and any session/conversation continuity actually implemented.

The authors must agree on prompt bytes, ordering, API/session semantics, repetition count, randomization if any, timeouts and permissible output classes **before** evaluating the results. The previously shared ellipsis example may be retained as exploratory historical evidence only if its native provenance is supplied; it is not a substitute for a newly executed preregistered condition.

## Non-interference and attribution

ÉLAN receives only its own approved prompts and ordinary native context, never StegVerse labels, policy targets or expected classifications. The StegVerse evidence chain is constructed independently after primary native traces are fixed. Each output is attributed to human annotation, client instrumentation, API response, or StegVerse derived representation. Preserve UNKNOWN / NOT_EXPOSED for anything not exposed in native traces. Retain errors and nonresults; never silently convert an API timeout into model silence.

## Comparison

First align actual invocation IDs and human-event timestamps. Then separately compare (a) native emitted output or API-level non-output/error, (b) externally supplied observation boundaries, (c) any *actually exposed* native decision representation, and (d) the downstream StegVerse controlled governance, custody, replay and reconstruction of independently represented events. Do not claim a native ÉLAN transition solely because a human timer advanced. Do not claim authentic live external InTr from controlled SDK results.

## Conditions before execution

- Both authors approve the exact protocol and independent attribution plan.
- Corrected source PDF is available and directly reconciled with the superseded historical source.
- Exact historical ellipsis trace provided if it will be referenced as evidence.
- Recording/retention permissions and public-release scope agreed separately from private correspondence.

## Publication

Both authors agreed to target the FAccT 2027 timeline (2026-10-27 abstract, 2026-11-03 full paper). This agreement is not manuscript approval, experimental success, abstract submission or acceptance. Keep the new HOLD study explicitly proposed until valid traces and author approvals exist.
