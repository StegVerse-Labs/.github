# SDK WorkSpace External-Collaboration Component Model Mirror Handoff

Updated: 2026-09-12
Goal Task ID: `SDK-WORKSPACE-EXTCOLLAB-AUTHENTIC-RUNTIME-004`
Canonical runtime handoff: `docs/SDK_WORKSPACE_EXTCOLLAB_AUTHENTIC_RUNTIME_004_MIRROR_HANDOFF.md`
COSV: `71000000100110`
Canonical model merge: `.github` PR #1652 / `b9f8e5153aa1651f2d7f043fb902eacb7c113ed9`
Status: `ACTIVE / REUSABLE TASK COMPONENT MODEL CANONICAL / GOAL COMPONENT PROFILE RECONCILED / AUTHENTIC RUNTIME EVIDENCE STILL REQUIRED`

## Identity and runtime truth

The existing Goal Task remains valid and unchanged. Componentization does not create a replacement Goal Task, reset prompt count, alter COSV continuity, or satisfy runtime predicates. Runtime truth remains exclusively in `docs/SDK_WORKSPACE_EXTCOLLAB_AUTHENTIC_RUNTIME_004_MIRROR_HANDOFF.md`.

Canonical Task Registry record:

```text
data/canonical-task-records/SDK-WORKSPACE-EXTCOLLAB-AUTHENTIC-RUNTIME-004.json
```

Canonical reusable composition profile:

```text
data/goal-task-component-profiles/SDK-WORKSPACE-EXTCOLLAB-AUTHENTIC-RUNTIME-004.json
```

Existing transport-family profile remains:

```text
data/goal-task-transport-profiles/SDK-WORKSPACE-EXTCOLLAB-AUTHENTIC-RUNTIME-004.json
```

## Decomposition decision

The canonical decomposition policy applies before further task-specific orchestration. The current process has these active signals:

- repeated subflows;
- multiple authority crossings;
- multiple governed round trips;
- cross-repository / cross-organization spread;
- long handoff sequence with independently evidenced stages;
- branching remediation paths;
- independently reusable subprocesses;
- optional subprocesses;
- independently provable evidence predicates.

The deterministic score is `25`, yielding:

```text
STOP_SCOPE_GROWTH_AND_DECOMPOSE_BEFORE_ADDING_MORE_TASK_SPECIFIC_ORCHESTRATION
```

`task_specific_adapter_duplicates_generic_work` is intentionally `false` in the profile because this reconciliation stops extension of generic behavior inside task-specific adapters and instead binds the existing adapters to reusable components.

## Goal Task -> reusable components -> owners -> evidence

The task composes only the capabilities it actually needs:

1. `RUNTIME-OBSERVATION-EXISTING` -> HeartBeat/canonical runtime-presence producer -> current exact-subject presence evidence. Required before resident dispatch.
2. `RTC-MANIFEST-001` -> coordination-only task/COSV manifest binding -> manifest validation evidence. Required.
3. `EXECUTION-MATERIALIZATION-EXISTING` -> reusable ephemeral-construct lifecycle + existing resident dispatch; WorkerCoordinator retains claim/fence and Interlock/InTr retains transition authority -> resident consumption or exact boundary receipt. Required after authentic runtime observation.
4. `RTC-GOVERNED-PROCESSING-002` -> Interlock/InTr -> governed processing/transition evidence. Required.
5. `RTC-ROUNDTRIP-003` -> repeatable non-authorizing transport component -> five declared instances: resident reseal, resident listener, sovereign callback, owner-present provider consent, authoritative provider probe. Required.
6. `CREDENTIAL-SESSION-EXISTING` -> TV/TVC with KV/SKAP Vault as sole user verifier -> custody/session/provider evidence. Required.
7. `CALLBACK-CORRELATION-OWNER-BINDING` -> existing Service Gateway `StegVerse-org/LLM-adapter#72` + TV/TVC session semantics + Interlock/InTr transition authority -> callback reachability/correlation evidence. Required; no duplicate callback listener/tunnel/session path is created.
8. `EVIDENCE-VALIDATION-EXISTING` -> existing SDK/TVC validation surfaces -> provider-probe classification, SDK predicate re-evaluation, MIR input. Required and non-authorizing.
9. `RTC-EVIDENCE-CUSTODY-004` -> Master Records -> durable custody/readback/reconstruction. Required.
10. `RTC-SDK-RETURN-006` -> non-authorizing return assembly -> SDK return/predicate artifact. Required.
11. `RTC-PUBLISHER-005` -> publication projection; TV/TVC retains release authority -> downstream/public-distribution evidence. Required for this Goal Task, although optional globally.
12. `RTC-STEGVERSE-EGRESS-007` -> Interlock/InTr -> final StegVerse-side transition receipt. Required.
13. `RTC-INTERLOCK-INTR-TRANSPORT-008` -> Interlock/InTr -> governed packet/transition receipts, repeatable where declared. Required.
14. `RTC-FARSIDE-FINAL-009` -> applicable far-side transition authority -> far-side final receipt. Required for this Goal Task's public-distribution path, although optional globally.
15. `FAILURE-REMEDIATION-EXISTING` -> existing StegDB -> StegHealth lifecycle/health path plus reusable stop-boundary semantics -> exact remediation/boundary evidence. Conditional.
16. `TERMINAL-CLEANUP-EXISTING` -> reusable ephemeral-construct lifecycle + Master Records terminal custody/reconstruction -> runner-expiry/residual-recording/entropy-recovery evidence. Conditional on ephemeral runner materialization.

The machine-readable profile records inputs, outputs, preconditions, evidence, cardinality, remediation semantics, and applicability for each component.

## Duplicate orchestration disposition

Historical evidence is preserved, but duplicate orchestration must not continue:

- PR #1578 is closed unmerged and superseded. Its task-local runtime-visibility/self-heal framing must not be revived; normal short-lived worker/WorkerCoordinator absence is not itself failure, and lifecycle/health interpretation belongs to the existing StegDB -> StegHealth path.
- The existing portable resident reseal/listener consumers remain valid only as task-parameterized adapters to reusable execution-materialization and round-trip components. They must not grow into a second runtime, scheduler, generic transport stack, credential owner, or liveness system.
- `heartbeat_runtime/runtime_presence_projection.py` remains the sole canonical runtime-presence producer.
- `StegVerse-org/LLM-adapter#72` remains the Service Gateway owner. No duplicate callback/public-route implementation is authorized.

No genuinely new reusable capability is identified by this reconciliation. Callback correlation, credential/session handling, evidence validation, remediation, and release/publication behavior are bound to existing canonical owners and reusable transport components instead of spawning new Goal Tasks or duplicate implementations.

## Authority separation

Reusable components are non-authorizing.

- Task Registry: coordination only.
- WorkerCoordinator: claim/fence authority.
- Interlock/InTr: governed transition/admission authority.
- TV/TVC: credential/provider/release authority.
- KV/SKAP Vault: sole user-verification authority.
- StegOS devices: interchangeable transport/execution nodes only.
- Master Records: observed-reality custody/reconstruction.
- HeartBeat: synchronization/timing/freshness/liveness/correlation/observability only.
- GitHub: source/evidence coordination only; no runtime authority.

Runtime subject binding, resident node identity, device identity, transport identity, and Secure Enclave identity remain evidence/transport identities only and never become user-verification authority.

## Goal Task-specific predicates preserved

Componentization does not alter or satisfy these predicates:

```text
RESIDENT_RESEAL_CONSUMPTION_RECEIPT_OBSERVED
RESIDENT_CONSENT_LISTENER_CONSUMPTION_RECEIPT_OBSERVED
EXTERNAL_COLLAB_CLIENT_SECRET_CUSTODY_PROVEN
SOVEREIGN_CALLBACK_REACHABILITY_PROVEN
OWNER_PRESENT_GOOGLE_CONSENT_PROVEN
AUTHORITATIVE_PROVIDER_FILE_PROBE_PROVEN
SDK_ACTIVE_PROBE_COMPLETE_PREDICATE_REEVALUATION_PROVEN
MIR_TRANSITION_REPORTING_PROVEN
MASTER_RECORDS_CUSTODY_RECONSTRUCTION_PROVEN
ONE_CURRENT_DEVICE_END_TO_END_PROVEN
DOWNSTREAM_PROPAGATION_COMPLETE
PUBLIC_DISTRIBUTIONS_COMPLETE
```

## Runtime/evidence state

The source architecture is componentized, but runtime evidence is unchanged. The canonical runtime record still reports `CURRENT_OBSERVATION_REQUIRED:DECLARED_ONLY` / `UNRESOLVED_NO_CURRENT_AUTHORIZED_DEVICE`, with no authentic resident reseal/listener receipt, target custody/readback, sovereign callback, owner-present consent, provider probe, Master Records reconstruction, propagation, or publication proof.

Source construction, reusable composition, CI, merge state, and static compatibility do not upgrade any runtime evidence class.

## Canonical model validation

PR #1652 merged at `b9f8e5153aa1651f2d7f043fb902eacb7c113ed9` from exact head `075b1e71d0ebe3591899db03d570da79eed5e916` after successful exact-head validation:

```text
Organization Control: 34730323940 PASS
Deterministic Repository Suite: 34730323942 PASS
Heartbeat Worker Project: 34730323876 PASS
validate-deepseek-resident: 34730323965 PASS
```

This validation proves source/process architecture only.

## Next admissible work

1. Continue to reuse the canonical current-runtime observation component; do not add task-local presence or persistent-worker machinery.
2. When authentic current presence appears with exact subject binding, enter the existing execution-materialization component and dispatch only the already-existing resident reseal/listener adapters.
3. Treat each resulting operation as a parameterized `RTC-ROUNDTRIP-003` instance and preserve exact boundary/failure receipts.
4. Continue through credential/session, callback, provider probe, validation, Master Records, SDK return, publication, governed egress/transport, and far-side transition only as each prior evidence and authority prerequisite is authentically satisfied.
5. Do not perform owner-present provider consent until target custody and sovereign callback reachability are proven.
6. Do not create a second user-operated device or device-local user verification path.

## README impact

The root README was updated by PR #1652 to project the canonical Reusable Task Component Model. No additional README change is required by this reconciliation because it only binds this existing Goal Task to that already-published architecture.

## Human action

None now.