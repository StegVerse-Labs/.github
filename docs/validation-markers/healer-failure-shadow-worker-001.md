# Healer failure-mailbox live-shadow worker validation marker

Validation-only marker. The worker, handoff, registry fragment, adapter fragment, and focused deterministic tests are already on `main` through `3a40067163710ff1dc4c05a13706b46fcee2c633`.

This marker exists only to trigger the existing non-authorizing Heartbeat Worker Project validation surface against the exact registered state.
